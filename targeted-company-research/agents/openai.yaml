interface:
  display_name: "Targeted Company Research"
  short_description: "Source-backed company deep dives for jobs, investment, ABM and diligence"
  default_prompt: "Use $targeted-company-research to create a local source-backed company deep-dive report for a named target company, including keyword matrix, sources, data gaps, Markdown, HTML and PDF."
policy:
  allow_implicit_invocation: true
